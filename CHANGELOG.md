# Changelog

## v6.10.2 (2026-08-04)

### 新增功能

- **回收站清理**：回收站从 Winapp2 规则中独立为单独的可选项，默认不勾选，仅自定义清理可用。不再随小白一键清自动清空。([#recycle-bin])
- **C 盘日志文件清理**：新增 `.log` 文件全盘递归清理选项，默认不勾选。与"临时文件清理"同时勾选时，自动合并为单次 C 盘遍历，避免重复扫描。([#log-files])
- **内存转储清理**：新增 `memory.dmp` 和 `Minidump/*.dmp` 崩溃转储文件清理，默认勾选，小白一键清包含。 ([#memory-dumps])
- **旧 Windows 残留清理**：新增 `windows.old`、`$windows.~bt`、`$windows.~ws` 大版本更新残留清理，默认勾选，小白一键清包含。 ([#old-windows])
- **Windows Update 彻底清理**：清理策略从"检测到更新服务运行中则跳过"升级为"暂停服务→彻底清理→恢复服务"。扩展清理范围至完整 SoftwareDistribution 目录及其备份、`$NtUninstallKB*` 补丁卸载器残留。 ([#update-cleanup])
- **递归清理四项**：自定义清理新增 `node_modules` 目录、散落 `Thumbs.db`、`.bak` 备份文件、编辑器临时文件 (`*~`) 四项全盘扫描选项，默认均不勾选。同时勾选多项时合并为单次遍历。 ([#deep-scan])
- **自定义清理记忆功能**：用户勾选的清理项目自动保存到本地，下次打开自定义清理窗口时恢复上次选择。重置默认后也会记住。 ([#memory])

### 修复

- 修复小白一键清会清空回收站的问题。

---

## v6.0.1

- 使用Rust重构WindowsCleaner
