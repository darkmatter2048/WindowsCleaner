name: 发行

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "版本号"
        required: true
        default: v6.0.1

jobs:
  Build:
    uses: ./.github/workflows/build.yml

  Release:
    needs: [Build]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 下载附件
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: 发行
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ inputs.tag }}
          tag_name: ${{ inputs.tag }}
          draft: false
          make_latest: true
          files: |
            artifacts/WindowsCleaner/WindowsCleaner.zip
